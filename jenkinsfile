pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git 'https://github.com/AjayKumarMekala/com.tesng.git'
            }
        }

        stage('Build') {
            steps {
                bat "mvn clean install"
            }
        }
    }
}
